{% extends 'azobe_base_front.html.twig' %}

{% block title 'Connexion  | ' ~ parent()%}

{% block registration %}

        {# Code du bon formulaire #}
			<section class="block-wrapper section-bg">
			<div class="container">
				<div class="row">
					<div class="col-lg-6 mx-auto">

						<div class="ts-grid-box">
							<div class="reg-page">
								<h3 class="log-sign-title mb-25 text-danger text-center">Inscrivez vous s'il vous plait 
									<i class="fa fa-edit text-danger" aria-hidden="true"></i></h3>
									{# Pour insérer le formulaire twig#}

										{# Code en attente #}

											{{ form_start(registrationForm) }}
										<div class="form-row">
												<div class="col form-group">
													{{form_row(registrationForm.prenom)}}
												</div>

												<div class="col form-group">
													{{form_row(registrationForm.nom_famille,{
													label:'Nom de famille'})}}
												</div>
										</div>
										<div class="form-group">
											{{ form_row(registrationForm.email,{
												label:'Adresse électronique'
											}) }}
										</div>

										<div class="form-row">
											<div class="form-group col-md-6">
												{{form_row(registrationForm.ville)}}
											</div>
											<div class="form-group col-md-6">
											{{form_row(registrationForm.pays,)}}
											</div>
										</div>
											
											{{ form_row(registrationForm.plainPassword, {
												label: 'Mot de passe'
											})}}
											

											{{ form_row(registrationForm.confirm_password, {
												label: 'Confirme mot de passe'
											}) }}

											{{ form_row(registrationForm.agreeTerms,{
												label:'Acceptez les conditions.'
											}) }}

											<button type="submit" class="btn">Register</button>
											{{ form_end(registrationForm) }}
									{#Fin insérer le formulaire twig #}
									<div class="border-top card-body text-center">Avez-vous déjà un compte? <a href="connexion.html">Se connecter</a></div>
							</div> <!-- card.// -->

						</div><!-- grid box end -->
					</div>
					<!-- col end-->
				</div>
				<!-- row end-->
			</div>
			<!-- container end-->
		</section>

			
					 		{# Code en attente #}

                            {# {{ form_start(registrationForm) }}
							{{ form_row(registrationForm.email) }}
							{{ form_row(registrationForm.plainPassword, {
								label: 'Password'
							}) }}
							{{ form_row(registrationForm.agreeTerms) }}

							<button type="submit" class="btn">Register</button>
							{{ form_end(registrationForm) }} #}
                    
       
        {# End Code du bon formulaire #}

		{# <form role="form">
            {{ form_start(registrationForm) }} #}
                                    
					{# <div class="form-group"> #}
						{# <input type="text" class="form-control" id="inputUsernameEmail" placeholder="Nom d'utilisateur ou email"> #}
                    		{# {{ form_row(registrationForm.email) }}
								</div>
								<div class="form-group"> #}
								{# <a class="pull-right" href="#">mot de passe oublié?</a> #}
								{# <input type="password" class="form-control" id="inputPassword" placeholder="Mot de passe"> #}
                                  {# {{ form_row(registrationForm.plainPassword, { label: 'Password'})}} #}
								{# </div> #}
                            
                                {# <div class="checkbox pull-right mr-4"> #}
                                    {# <label> #}
                                       
                                       {# {{ form_row(registrationForm.agreeTerms) }} #}
                                    {# </label> #}
								{# </div> #}
                                {# <br> #}
								{# <div class="d-flex  ts-submit-btn"> #}
							{# <button class="btn btn-primary">Se connecter</button> #}
					{# </div> #}
			{# </form> #}

{% endblock %}
